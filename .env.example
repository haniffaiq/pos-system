# BroSolution / Operational Grosir environment template
# Copy to .env for local development. Keep .env untracked and replace every change_me_* placeholder before production.

# Postgres — shared instance (credentials supplied by the instance owner)
DATABASE_URL=postgresql://change_me_user:change_me_password@postgres:5432/change_me_db

# Redis / queues / exports
REDIS_URL=redis://change_me_user:change_me_password@redis:6379/0
APP_NAMESPACE=change_me_namespace
EXPORT_DIR=/data/exports

# Auth / tokens
JWT_ACCESS_SECRET=change_me_48_byte_base64_access_signing_key
JWT_REFRESH_SECRET=change_me_48_byte_base64_refresh_signing_key
ACCESS_TOKEN_TTL=900
REFRESH_TOKEN_TTL=2592000
INVITE_TOKEN_TTL_SECONDS=604800
PASSWORD_RESET_TOKEN_TTL_SECONDS=3600

# P3 auth hardening: HTTP-only cookie/session semantics
# Generate MFA_KMS_KEY: openssl rand -base64 32
MFA_KMS_KEY=change_me_32_byte_base64_mfa_kms_key
SESSION_COOKIE_NAME=brosolution_session
SESSION_COOKIE_DOMAIN=
SESSION_COOKIE_SECURE=false
CSRF_COOKIE_NAME=brosolution_csrf
CSRF_HEADER_NAME=x-csrf-token
# Development/test only. API startup refuses this variable in production-like environments.
AUTH_MFA_BYPASS_EMAILS=
MFA_CHALLENGE_RATE_LIMIT_POINTS=5
MFA_CHALLENGE_RATE_LIMIT_SECONDS=60
MFA_CHALLENGE_MAX_FAILURES=5
# Set RATE_LIMIT_DISABLED=true only in local development to disable Redis rate limiters
RATE_LIMIT_DISABLED=false

# SMTP / transactional email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=BroSolution <no-reply@localhost>
MAIL_FROM=BroSolution <no-reply@localhost>
MAILHOG_WEB_PORT=8025

# App
API_PORT=4000
WEB_PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:4000
PUBLIC_APP_URL=http://localhost:3001
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001

# Shared Docker network (name supplied by the instance owner) so the
# postgres / redis / minio hostnames resolve.
SHARED_NETWORK=change_me_shared_network

# Production reverse proxy (P8)
DOMAIN=brosolution.example.com
ACME_EMAIL=ops@example.com

# Billing PSPs (P5) — set keys per provider; runtime falls back when active PSP config is incomplete
BILLING_ENABLED=false
BILLING_ACTIVE_PSP=midtrans
MIDTRANS_ENV=sandbox
MIDTRANS_SERVER_KEY=
MIDTRANS_CLIENT_KEY=
MIDTRANS_MERCHANT_ID=
XENDIT_ENV=sandbox
XENDIT_SECRET_KEY=
XENDIT_PUBLIC_KEY=
XENDIT_WEBHOOK_TOKEN=

# Observability (P1)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_RELEASE=
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development
NEXT_PUBLIC_SENTRY_RELEASE=
LOG_LEVEL=info
PROMETHEUS_METRICS_ENABLED=true
LOKI_URL=http://loki:3100
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change_me_grafana_password
GRAFANA_BIND=127.0.0.1:3001
PROMETHEUS_BIND=127.0.0.1:9090
LOKI_BIND=127.0.0.1:3100
PROMETHEUS_RETENTION=30d

# MinIO — shared object storage, used as the database backup target
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=change_me_minio_access_key
MINIO_SECRET_KEY=change_me_minio_secret_key
MINIO_BUCKET=change_me_bucket
BACKUP_RETENTION_DAYS=30

# E2E / Playwright helpers
E2E_ADMIN_EMAIL=admin@local
E2E_ADMIN_PASSWORD=replace-me-admin-password
E2E_BASE_URL=http://localhost:3001
E2E_GROSIR_OWNER_PASSWORD=replace-me-owner-password
E2E_GROSIR_SLUG=demo
PLAYWRIGHT_BASE_URL=http://localhost:3001
